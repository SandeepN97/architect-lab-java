# Rate Limiter Lab

Compare fixed-window, sliding-window, and token-bucket behavior by sending simulated traffic through the Command Center.

Learning goals:

- Understand burst handling and fairness.
- Observe allowed vs rejected requests.
- See how cache availability and injected latency affect p95 latency.
- Connect admin commands to audit-style events and metrics.
